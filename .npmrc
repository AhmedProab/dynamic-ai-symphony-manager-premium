# npm configuration
# Prevents permission issues

# Don't use sudo with npm
unsafe-perm=true

# Set npm cache location
cache=/Users/ahmedabdullah/.npm

# Registry
registry=https://registry.npmjs.org/

# Disable package-lock in global installs
package-lock=false

# Progress bar
progress=true

# Log level
loglevel=warn